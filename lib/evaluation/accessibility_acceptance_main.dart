import 'settings_acceptance_main.dart' as acceptance;

/// Manual #28 review only: a fresh isolated vault with fictional dense content.
/// Never imported by the production entry point.
void main() => acceptance.runAcceptance(accessibility: true);
